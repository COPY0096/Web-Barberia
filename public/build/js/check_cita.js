import{DateTime}from"luxon";function checkCita(e,t){let a=DateTime.now(),r=t.fecha.plus({minutes:30*t.servicios});const i=DateTime.fromObject({year:a.year,month:a.month,day:a.day,hour:8,minute:0}),o=DateTime.fromObject({year:a.year,month:a.month,day:a.day,hour:18,minute:0});if(t.fecha.weekday>5)return!1;if((t.fecha<i||t.fecha>o)&&r<o)return!1;for(let i of e){let e=i.fecha.plus({minutes:30*i.servicios});if(e>a&&e>t.fecha&&i.fecha<r)return!1}return!0}export{checkCita};